name: epl_pipeline
version: '1.0.0'
config-version: 2

profile: epl_pipeline

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

vars:
  project_id: cedar-style-487221-a3
  bq_location: US

models:
  epl_pipeline:
    staging:
      +materialized: view
      +schema: epl_staging
    mart:
      +materialized: table
      +schema: epl_mart
